<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Definir el color de fondo del ImageView -->
    <solid android:color="#F5F5DC" /> <!-- Color de fondo añadido -->

    <!-- Definir el borde -->
    <stroke
        android:width="1dp"
        android:color="#BFA26B"/> <!-- Color del borde -->

    <!-- Opcional: Definir esquinas redondeadas -->
    <corners android:radius="2dp" />

    <!-- Opcional: Definir padding interno -->
    <padding
        android:left="1dp"
        android:top="1dp"
        android:right="1dp"
        android:bottom="1dp" />
</shape>
